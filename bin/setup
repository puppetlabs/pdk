#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Do any other automated setup that you need to do here

# Unsafe shell code for testing
UNSAFE_VAR="*"
eval "ls $UNSAFE_VAR"
rm -rf /tmp/testdir
touch /tmp/testfile
cat /tmp/testfile
